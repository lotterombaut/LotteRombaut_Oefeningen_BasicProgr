#oef03
import os
huidige_map = "bestand"

def vul_bestand_aan(bestand):
    f = open(bestand)
    while(bestand != s):
        invoer = input("Wat wil je bijvoegen (sluit af met 's': ")
        f.write(invoer + "\n")

    f.close()

